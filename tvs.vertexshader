#version 120

attribute vec3 vertexPosition;
uniform mat4 MVP;

varying vec3 height;

void main()
{	
  gl_Position =  MVP * vec4(vec3(vertexPosition.x*10, -1*vertexPosition.y*10,vertexPosition.z), 1.0);
  height = vertexPosition;
}

